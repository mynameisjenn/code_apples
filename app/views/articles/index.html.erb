<div class="container">

  <div class="row">
    <div class="col s6">
      <h4>Jennifer Hatton</h4>
        <p>Hi, I'm Jennifer. Welcome to my blog. I love working in Ruby - it's my favorite language. My new motto is a code apple a day, keeps synaptic pruning away.</p>
        <%= link_to 'New article', new_article_path %>
    </div>
 
  <div class="col s6">
  <table class="highlight responsive-table">
    <h4>Posts</h4>
      <% @articles.each do |article| %>
        <tr>
         <td><%= article.title %></td>
         <td><%= article.text.truncate(30) %></td>
          <td><%= link_to 'Read', article_path(article) %></td>
        </tr>
        <% end %>
      </div>
    </div>
  </table>
</div>